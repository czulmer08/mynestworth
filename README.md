# mynestworth — PUBLIC deployment target (generated)

This repo serves the app to users. It contains ONLY:
- the served app file (the minified build from nestworth-dev, e.g. app.html or index.html)
- Privacy Policy + Terms + a homepage (required for Google OAuth verification)
- favicon / icons

Do NOT put source, tests, or internal docs here. The app file is generated by
`build-production.py` in nestworth-dev — never hand-edit it. After deploying, record the
push in nestworth-dev's DEPLOYED.md (public.* + confirmed date).
